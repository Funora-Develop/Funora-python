# Что Funora сегодня не может

Список закрытых вопросов лежит в [observations.md](observations.md), список
открытых - в [protocol-questions.md](protocol-questions.md). Здесь третье:
границы, которые не закрываются вопросом к площадке, потому что упираются не в
незнание, а в устройство наблюдения.

Документ существует ради одного: чтобы человек, выбирающий Funora, узнал об этих
границах до того, как построит на них бота, а не после.

## Состояний заказа известно два, а бывает их больше

Читаются `paid` («Оплачен») и `closed` («Закрыт»). Возврат, спор и отказ
существуют - продавец их видел, - но в снимок не попали, и носителей для них
никто не наблюдал.

Заказ в третьем состоянии даёт **ненаблюдённое значение**, а не `unknown` и не
ближайшее похожее. Обработчик, написанный как «если не closed, значит ждём
выдачи», на таком заказе поведёт себя не так, как задумывал автор. Правильная
форма - спрашивать про конкретное состояние и отдельно обрабатывать случай, когда
состояние не прочитано.

Снимается снимком страницы с заказом в третьем состоянии. До тех пор носитель
сохраняется в `status_carrier` как есть - именно по нему и станет видно, как
выглядит новое состояние.

### Отдельно: `paid` - это не финансовое подтверждение

Значение `paid` означает ровно одно: **площадка показывает состояние оплаты в
разделе продаж**. Это тот самый раздел, куда площадка сама отправляет продавцов
проверять оплату вместо того, чтобы верить сообщениям в чате. Но подтверждением
перевода средств оно не является, отменённым задним числом быть может, и там, где
цена ошибки высока, собственную проверку не заменяет.

Поле называется `status`, а не `is_paid`, намеренно. Логическое поле склоняет к
записи `if order.is_paid:` и прячет третий случай - тот, где состояние не
прочитано.

## Маскирование путей в снимках - правило, а не доказательство

Для текста и значений атрибутов невозможность утечки - свойство формата:
хранится подпись вида `T14:cdps`, обратного преобразования не существует.

С путями иначе. Там работает правило: на своём хосте сохраняется форма пути, на
чужом путь маскируется целиком. Правило подобрано так, чтобы личное не проходило,
и проверяется тестами по опубликованным файлам - но это подбор, а не
доказательство. Ошибка в правиле выражается утечкой, и заметить её можно только
глядя на результат.

Второе следствие того же правила касалось идентификаторов и снято.

До формата v5 маска схлопывала идентификаторы: все заказы снимка несли один
адрес, все пятьдесят диалогов - одну подпись. Проверка, зависящая от их
различимости, проходила впустую, выглядя пройденной. В v5 нумеруются пути,
строки запроса и значения атрибутов - идентификатор прячется в каждом из трёх.
Номер не раскрывает значения: он говорит только, совпадают ли два.

Снимки прежних версий в v5 не преобразуются: из `{n}` и из `T9:d` исходное
значение не вернуть. Лежащие в репозитории сняты заново - кроме гостевой
страницы: её нельзя переснять по желанию, она снялась тогда, когда сессия истекла
сама.

Нумерация вводит ловушку взамен. Между документами номера не связаны: один и тот
же заказ получает разные номера в двух снимках, а разные заказы - одинаковые.
Сравнивать снимки по номерам нельзя.

Отсюда обязанность, которую нельзя снять с человека: **перед публикацией снимка
посмотреть на него глазами.** Инструмент не сохраняет сырой HTML никогда, но
последнюю проверку делает не он.

## Подделку сообщения точной глубины отличить нечем

Разбор переписки определяет происхождение сообщения по разметке: у сообщения
площадки есть обёртка предупреждения и нет ссылки на автора. Разметку в тексте
сообщения пишет собеседник, и если площадка не экранирует угловые скобки, он
может закрыть открытые элементы и открыть поддельное сообщение.

Несбалансированная попытка ловится: закрыто меньше нужного - подделка внутри
настоящего сообщения, больше - вне контейнера, оба случая дают повреждение
уровня страницы.

**Попытка с точным числом не ловится.** Закрыв ровно столько элементов, сколько
лежит между текстом и контейнером, отправитель получает сообщение, которое
является законным прямым потомком контейнера и структурно неотличимо от
настоящего. Следа вставки в разобранном дереве не остаётся.

Экранирует ли площадка текст, по снимку не проверить: подпись не различает
угловых скобок. Косвенно на экранирование указывает то, что ссылки в сообщениях
собеседников построены площадкой из простого текста.

Отсюда правило, которое не отменяется ничем: **происхождение `system` не
является подтверждением оплаты.** Проверка формы поднимает цену подделки, но
последним рубежом остаётся именно это правило - и раздел продаж, куда площадка
сама отправляет продавцов проверять оплату.

## Вердикты по текстовым признакам - всегда догадка

Классификация ответа идёт по конвейеру, порядок шагов нормативен. Текстовые
признаки стоят в нём последними и применяются **только к неопознанным
страницам**: если страница узнана по разметке, текст не смотрится вовсе.

Это ограничивает вред, но не отменяет природу шага. Текст локализован, площадка
вправе его переписать, а совпадение подстроки не доказывает ничего о состоянии
сессии. Любой вердикт, полученный текстовым признаком, - предположение, и
помечен он соответствующей уверенностью.

Первая версия признака `login_form` была именно таким предположением и оказалась
неверной: подтверждённый `guest_navbar` заменил её после первого же снимка
гостевой страницы. Остальные текстовые признаки просто ещё не проверялись.

## Событие, случившееся и откатившееся во время простоя, не наблюдаемо

Funora не диффит снимки друг с другом: она держит курсор - множество известных
идентификаторов - и объявляет событием появление неизвестного. Такая схема
переживает пропущенные опросы и перезапуски, чего диффинг снимков не умеет.

Чего не умеет и она: заказ, который возник и исчез между двумя опросами, не
оставит следа. То же с сообщением, удалённым до следующего обращения.

Это не дефект реализации, а свойство наблюдения через опрос: между опросами
источника данных нет. Доставка обещана **как минимум однократная** - каждое
событие, дожившее до следующего опроса, будет доставлено хотя бы раз, - и ровно
это, ни больше.

Отсюда практическое: обработчик обязан быть идемпотентным. Дедупликация в Funora
есть, она переживает перезапуск, но выстроена как оптимизация поверх обещания «не
меньше одного раза», а не вместо него.

## Из шестнадцати видов событий порождаются семь

Перечисление `EventType` объявляет шестнадцать видов; эта реализация порождает
семь: `message.created`, `chat.unread_changed`, `order.created`,
`order.status_changed`, `watch.primed`, `snapshot.incomplete`, `event.loss`.

Остальные девять - отзывы, лоты, предложения на витрине, здоровье протокола
- объявлены контрактом и не порождаются никем. Не «пока не порождаются в
интересном случае», а не порождаются вовсе: кода, который их создаёт, нет.

Подписка на такой вид **отвергается при регистрации**, а не принимается молча.
Молчание здесь неотличимо от «ничего не произошло»: продавец, подписавшийся на
отзывы, увидел бы ровно то же, что при отсутствии новых отзывов, и узнал бы об
ошибке в тот день, когда отзыв придёт.

Практическое следствие для вызывающего: если ваш код подписывался на один из
одиннадцати, он теперь падает при запуске. Это и есть починка - раньше он
работал молча и вхолостую.

Перечень порождаемого проверяется против снимков: объявить вид и не порождать
его значило бы вернуться к тому же молчанию с разрешения.

## Из двадцати двух возможностей выполняются три

`orders.list`, `chats.list`, `chats.history`. Под остальные девятнадцать в SDK
нет ни одного метода.

Таблица начальных состояний отвечает на вопрос о **площадке**: наблюдается ли
возможность там. Вызывающий спрашивает другое - «могу ли я это вызвать», - и
одиннадцать возможностей отвечали ему `supported`, то есть «подтверждена и
доступна», при том что метода под них нет вовсе.

Теперь `client.capability()` для невыполняемой возможности отвергает вызов
вслух. Тот же довод, по которому отвергается подписка на непорождаемое событие.

**Состояние нельзя привести к булеву значению.** Запись
`if client.capability(cap): call()` выглядит естественно, а состояние это строка,
и любая непустая строка истинна: проверка пропускала вызов при `unsupported` -
ровно тот случай, ради которого она и написана. Тот же запрет стоит у `Observed`
и по той же причине.

## Прокси меняет сетевую идентичность, а не отменяет ограничение

Прокси можно объявить один или несколько. Аккаунт привязывается к первому
пригодному и держится его: аккаунт, ходивший с одного адреса и вдруг сменивший
его, выглядит иначе, чем аккаунт с постоянным адресом.

Спецификация привязывает бюджет к сетевой идентичности - паре «исходящий адрес,
целевой хост». Прокси меняет первую половину пары, то есть даёт **другую**
идентичность: со своим запасом токенов, своим остыванием и своим счётом
ограничений.

**Переключение не отменяет отступления.** Прокси, получивший `429`, остывает по
объявленному правилу независимо от того, ушла работа на другой или нет:
вернуться к нему раньше срока нельзя. Иначе переключение означало бы
«продолжать в прежнем темпе с другого адреса», а не «дать этому адресу
отдохнуть».

Когда остывают все объявленные прокси, клиент **отказывает вслух**, а не
возвращается к прямому соединению: прямое соединение раскрыло бы адрес, который
вызывающий намеренно прятал, и сделало бы это ровно в тот момент, когда он этого
меньше всего ждёт.

Схема без шифрования до прокси отвергается: секрет уходит в заголовке каждого
запроса, и `http://` означает, что ключ читает любой на пути.

**О рисках.** Смена адреса вслед за ограничением частоты - признак, по которому
площадки и опознают автоматизацию. Здесь этот риск принят владельцем проекта
сознательно, и отступление сохранено именно поэтому: работа уходит на другой
адрес, а первый честно остывает.

## Реакция на ограничение частоты

Три ступени. Первое ограничение режет ёмкость ведра вдвое и даёт остыть минуту;
второе в том же окне - вчетверо и две минуты; дальше ёмкость не опускается ниже
одной восьмой. Восстановление медленнее падения: десятая доля ёмкости за каждые
двадцать успешных запросов подряд, и любое новое ограничение обнуляет счёт.

Несимметричность намеренна. Симметричное восстановление даёт автоколебания:
система отступает, тут же возвращается к прежней частоте, получает ограничение
снова и так по кругу.

Прежде реакция была объявлена спецификацией и не выполнялась нигде: ответ `429`
переводился в ошибку и уходил в политику повторов, а ёмкость оставалась
прежней - следующий залп был ровно таким же, каким был до ограничения.

## Целостность тела проверяется только без сжатия

Библиотека распаковывает ответ прозрачно, а заголовок `Content-Length` объявляет
длину **сжатого** тела. Проверка целостности сравнивала распакованную длину с
объявленной сжатой - двести тысяч байт против двухсот пятидесяти - и проходила
всегда, в том числе на оборванном ответе.

Распаковка обрыв тоже не ловит: оборванный gzip разворачивается частично и без
ошибки. Проверено - половина потока дала 88 тысяч байт правдоподобного текста.

Поэтому клиент просит сервер не сжимать: `Accept-Encoding: identity`. Цена -
трафик; запросов от этого больше не становится, а страница, оборванная посреди
таблицы, проходит и классификацию как пригодную, и разбор как полный.

Если сервер просьбу не выполнит, целостность **не проверяется** и в журнал
уходит предупреждение. Сравнить несжатое со сжатым и объявить целостность
подтверждённой было бы хуже, чем признаться.

## Предел времени работает только у асинхронных обработчиков

Сопрограмму можно отменить, обычную функцию - нет: прервать выполняющийся код в
Python нечем, кроме отдельного потока на каждый вызов, а это меняет модель
исполнения обработчика.

Синхронный обработчик, ушедший в вечное ожидание, останавливает цикл наблюдения.
Асинхронный отпускается через `handler_timeout_ms` и получает
`HandlerTimeoutError`, а наблюдение продолжается.

## Что контракт объявляет и реализация не делает

Перечень живёт в `spec/conformance/not-implemented.yaml` и сверяется проверкой:
объявленное и нечитаемое обязано быть в нём, реализованное обязано из него
исчезнуть. Сегодня там четыре механизма - классы запросов с долями, реакция на
ограничение площадки, залповый предел ведра и признаки счёта повторов.

Перечень появился потому, что все четыре выглядели частью контракта и не делали
ничего. Автор второго SDK, читающий спецификацию, не станет писать свою защиту от
вытеснения, если контракт обещает доли.

## Канал обновлений площадки найден, но не используется

У площадки есть собственный канал: `POST https://funpay.com/runner/`, промежуток
5 секунд. Наблюдался срабатывающим на входящее сообщение, и отдаёт он не признак
«что-то поменялось», а сами данные - счётчик числом, порядок списка диалогов и
готовую разметку раздела.

Funora к нему не обращается, и причина не в лени. Неизвестно, что канал делает
при истёкшей сессии и при исчерпании лимита - ровно те два случая, ради которых в
SDK написаны политика повторов и распознавание состояния сессии. Строить на
источнике, поведения которого в отказе мы не видели, значило бы заменить
медленный и понятный опрос быстрым и непонятным.

Практическое следствие: изменение замечается за время опроса страницы, а не за
пять секунд. И счётчик непрочитанного остаётся выведенным из расхождения двух
позиций, тогда как канал отдаёт его числом.

Подробности и то, чем вопрос закрывается, - в
[protocol-questions.md](protocol-questions.md).
